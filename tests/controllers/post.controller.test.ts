import { jest, describe, expect, beforeAll, afterAll, it } from '@jest/globals';

const mockReply = {
    send: jest.fn(),
};
  

describe('PostController', () => {
  it('should return posts by typeId', async () => {
    
  });
});
